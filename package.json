{
  "name": "pocket-skipper",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/unit/*.test.mjs\"",
    "check": "node scripts/check-content.mjs",
    "assets": "node scripts/update-sw-assets.mjs",
    "serve": "python3 -m http.server ${PORT:-4173} --bind 127.0.0.1 --directory site",
    "e2e": "playwright test",
    "shots": "node scripts/shoot-maneuvers.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
